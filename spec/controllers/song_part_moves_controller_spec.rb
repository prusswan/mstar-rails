require 'spec_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to specify the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator.  If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails.  There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.
#
# Compared to earlier versions of this generator, there is very limited use of
# stubs and message expectations in this spec.  Stubs are only used when there
# is no simpler way to get a handle on the object needed for the example.
# Message expectations are only used when there is no simpler way to specify
# that an instance is receiving a specific message.

describe SongPartMovesController do

  # This should return the minimal set of attributes required to create a valid
  # SongPartMove. As you add validations to SongPartMove, be sure to
  # update the return value of this method accordingly.
  def valid_attributes
    { "song_part_id" => "1" }
  end

  # This should return the minimal set of values that should be in the session
  # in order to pass any filters (e.g. authentication) defined in
  # SongPartMovesController. Be sure to keep this updated too.
  def valid_session
    {}
  end

  describe "GET index" do
    it "assigns all song_part_moves as @song_part_moves" do
      song_part_move = SongPartMove.create! valid_attributes
      get :index, {}, valid_session
      assigns(:song_part_moves).should eq([song_part_move])
    end
  end

  describe "GET show" do
    it "assigns the requested song_part_move as @song_part_move" do
      song_part_move = SongPartMove.create! valid_attributes
      get :show, {:id => song_part_move.to_param}, valid_session
      assigns(:song_part_move).should eq(song_part_move)
    end
  end

  describe "GET new" do
    it "assigns a new song_part_move as @song_part_move" do
      get :new, {}, valid_session
      assigns(:song_part_move).should be_a_new(SongPartMove)
    end
  end

  describe "GET edit" do
    it "assigns the requested song_part_move as @song_part_move" do
      song_part_move = SongPartMove.create! valid_attributes
      get :edit, {:id => song_part_move.to_param}, valid_session
      assigns(:song_part_move).should eq(song_part_move)
    end
  end

  describe "POST create" do
    describe "with valid params" do
      it "creates a new SongPartMove" do
        expect {
          post :create, {:song_part_move => valid_attributes}, valid_session
        }.to change(SongPartMove, :count).by(1)
      end

      it "assigns a newly created song_part_move as @song_part_move" do
        post :create, {:song_part_move => valid_attributes}, valid_session
        assigns(:song_part_move).should be_a(SongPartMove)
        assigns(:song_part_move).should be_persisted
      end

      it "redirects to the created song_part_move" do
        post :create, {:song_part_move => valid_attributes}, valid_session
        response.should redirect_to(SongPartMove.last)
      end
    end

    describe "with invalid params" do
      it "assigns a newly created but unsaved song_part_move as @song_part_move" do
        # Trigger the behavior that occurs when invalid params are submitted
        SongPartMove.any_instance.stub(:save).and_return(false)
        post :create, {:song_part_move => { "song_part_id" => "invalid value" }}, valid_session
        assigns(:song_part_move).should be_a_new(SongPartMove)
      end

      it "re-renders the 'new' template" do
        # Trigger the behavior that occurs when invalid params are submitted
        SongPartMove.any_instance.stub(:save).and_return(false)
        post :create, {:song_part_move => { "song_part_id" => "invalid value" }}, valid_session
        response.should render_template("new")
      end
    end
  end

  describe "PUT update" do
    describe "with valid params" do
      it "updates the requested song_part_move" do
        song_part_move = SongPartMove.create! valid_attributes
        # Assuming there are no other song_part_moves in the database, this
        # specifies that the SongPartMove created on the previous line
        # receives the :update_attributes message with whatever params are
        # submitted in the request.
        SongPartMove.any_instance.should_receive(:update_attributes).with({ "song_part_id" => "1" })
        put :update, {:id => song_part_move.to_param, :song_part_move => { "song_part_id" => "1" }}, valid_session
      end

      it "assigns the requested song_part_move as @song_part_move" do
        song_part_move = SongPartMove.create! valid_attributes
        put :update, {:id => song_part_move.to_param, :song_part_move => valid_attributes}, valid_session
        assigns(:song_part_move).should eq(song_part_move)
      end

      it "redirects to the song_part_move" do
        song_part_move = SongPartMove.create! valid_attributes
        put :update, {:id => song_part_move.to_param, :song_part_move => valid_attributes}, valid_session
        response.should redirect_to(song_part_move)
      end
    end

    describe "with invalid params" do
      it "assigns the song_part_move as @song_part_move" do
        song_part_move = SongPartMove.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        SongPartMove.any_instance.stub(:save).and_return(false)
        put :update, {:id => song_part_move.to_param, :song_part_move => { "song_part_id" => "invalid value" }}, valid_session
        assigns(:song_part_move).should eq(song_part_move)
      end

      it "re-renders the 'edit' template" do
        song_part_move = SongPartMove.create! valid_attributes
        # Trigger the behavior that occurs when invalid params are submitted
        SongPartMove.any_instance.stub(:save).and_return(false)
        put :update, {:id => song_part_move.to_param, :song_part_move => { "song_part_id" => "invalid value" }}, valid_session
        response.should render_template("edit")
      end
    end
  end

  describe "DELETE destroy" do
    it "destroys the requested song_part_move" do
      song_part_move = SongPartMove.create! valid_attributes
      expect {
        delete :destroy, {:id => song_part_move.to_param}, valid_session
      }.to change(SongPartMove, :count).by(-1)
    end

    it "redirects to the song_part_moves list" do
      song_part_move = SongPartMove.create! valid_attributes
      delete :destroy, {:id => song_part_move.to_param}, valid_session
      response.should redirect_to(song_part_moves_url)
    end
  end

end
